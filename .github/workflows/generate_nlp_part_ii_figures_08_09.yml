name: Generate NLP Part II Figures 8 and 9
on:
  push:
    paths:
      - "chapters/nlp_part_ii/case_studies/bert_imdb_sentiment/shared_experiment/run_figures_08_09.py"
      - ".github/workflows/generate_nlp_part_ii_figures_08_09.yml"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: |
          python -m pip install --upgrade pip
          pip install "torch>=2.2,<3" "transformers>=4.44,<5" "datasets>=3,<5" "accelerate>=1,<2" "scikit-learn>=1.4,<2" "pandas>=2.2,<3" "matplotlib>=3.8,<4" "huggingface_hub>=0.24,<1"
      - run: python chapters/nlp_part_ii/case_studies/bert_imdb_sentiment/shared_experiment/run_figures_08_09.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add chapters/nlp_part_ii/case_studies/bert_imdb_sentiment/shared_experiment/ chapters/nlp_part_ii/case_studies/bert_imdb_sentiment/figure_08_confusion_matrix/ chapters/nlp_part_ii/case_studies/bert_imdb_sentiment/figure_09_loss_curves/
          if git diff --cached --quiet; then echo "No generated changes."; else git commit -m "Generate NLP Part II Figures 8 and 9 from shared IMDb experiment"; git push; fi