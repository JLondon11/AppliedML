"""Rebuild revised Figure 29 architecture panel.
The empirical/keypoint panel is source-supported raster evidence and is not regenerated here.
See manifest.json for provenance and source constraints.
"""
# The production code for Figure 29 is preserved in the book-editing production package.
# This repository version intentionally does not synthesize or redraw the empirical keypoint-output panel.
# Rebuild the deterministic architecture with Matplotlib following the dimensions documented in manifest.json,
# then combine it with the source-supported keypoint panel without modifying scientific pixels.
