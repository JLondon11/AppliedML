# Figure 19 — Structure-Preserving Generative Image Editing

Application figure using authentic sklearn digits observations. A trained autoencoder is edited through latent optimization with separate penalties for source preservation outside the mask and structural control inside the mask.

Run `python src/figure19.py`. PNG, SVG, samples, measured errors, and training log are written to `outputs/`.
