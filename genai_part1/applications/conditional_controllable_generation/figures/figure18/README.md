# Figure 18 — Conditional Generation: Control–Fidelity Response

Application figure using authentic sklearn digits data and actual conditional-VAE/classifier training under seed 18018.

Run `python src/figure18.py`. PNG, SVG, numerical results, and training log are written to `outputs/`.
