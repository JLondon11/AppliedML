# Figure Reproduction

## Figure 15

Run from the repository root:

```bash
python -m pip install -r genai_part1/requirements.txt
python genai_part1/figures/figure15_quality_diversity.py
```

Outputs are written to `genai_part1/figures/outputs/`.

Figure 15 is a controlled distribution experiment generated by executable code. Its condition names describe controlled distribution behavior; they must not be relabeled as VAE, GAN, diffusion, or other real model families without protocol-matched experiments or traceable published measurements.

Future empirical figures in this directory must preserve the same provenance rule.
